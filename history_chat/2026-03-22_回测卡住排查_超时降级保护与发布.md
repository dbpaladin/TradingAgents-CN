# 聊天记录归档

- 归档时间：2026-03-22
- 主题：回测长时间卡住排查、后端防卡死修复、文档更新与发布
- 项目路径：`/home/wing/myproject/TradingAgents-CN`

## 用户诉求

用户反馈回测任务长时间运行不结束（界面停在低进度），希望定位问题并进行修复或优化；随后要求归档文件、更新文档、更新 GitHub。

## 排查结论

综合代码与任务状态确认，核心风险点为：

- 回测单日 AI 分析调用缺少任务级硬超时
- 一旦某日分析调用阻塞，会导致整条回测任务长时间停在 `running`
- 环境中存在推荐模型 API Key 占位符/缺失场景，易触发无效等待

## 已实施修复

### 1) 回测防卡死机制（核心）

文件：

- [app/services/backtest_service.py](/home/wing/myproject/TradingAgents-CN/app/services/backtest_service.py)

变更：

- 增加单日 AI 分析硬超时（按模型 timeout 推导并限幅）
- 分析超时后自动重建 `ThreadPoolExecutor`
- 连续超时达阈值后自动降级为 `HOLD`，保证任务可收敛
- 回测启动时提前初始化分析运行时，尽早暴露模型配置问题
- 当模型供应商需要 API Key 且缺失时，直接降级为 `HOLD`（快速失败）

### 2) 回归测试补充

文件：

- [tests/test_backtest_service.py](/home/wing/myproject/TradingAgents-CN/tests/test_backtest_service.py)

新增测试覆盖：

- 缺失 API Key 触发自动降级
- 连续超时触发 AI 停用降级

验证结果：

- `./.venv/bin/pytest -q tests/test_backtest_service.py`
- 结果：`38 passed`

### 3) 任务状态处理

已将长时间卡住任务标记为取消（保留记录，避免前端持续显示运行中）：

- `03c5c448-3fed-46b0-9128-8f2fd302116f` -> `cancelled`

## 文档更新

文件：

- [docs/fixes/backtest-iteration-updates-2026-03.md](/home/wing/myproject/TradingAgents-CN/docs/fixes/backtest-iteration-updates-2026-03.md)

更新内容：

- 新增“第五阶段：稳定性与防卡死加固（2026-03-22）”章节
- 说明超时、线程池自愈、连续超时降级、缺失 Key 快速降级等策略

## 发布说明

本次发布聚焦“稳定跑完”能力，优先消除回测假运行与长期卡住问题。后续可以继续按真实交易策略目标微调降级阈值和进度提示文案。
